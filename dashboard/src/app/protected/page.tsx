import React from 'react'

const Protected = () => {
  return (
    <div>This is a protected page, only authenticated users are allowed to see it.  FOR DEV PURPOSE</div>
  )
}

export default Protected